import * as React from 'react';
import '../index.css';

const VideoFigure = (props) => {
  
  const data = props.data;

  return (
    <figure>
      video
    </figure>
  );
}

export default VideoFigure;
